  ipv4 {
    import filter {
      reject;
    };

    export filter {
      rc_export();    
    };
  };
  ipv6 {
    import filter {
      reject;
    };

    export filter {
      rc_export();    
    };
  };
